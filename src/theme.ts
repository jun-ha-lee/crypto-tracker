import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = { // DefaultTheme의 타입을 가지게 된다 (styled.d.ts파일)
  bgColor: '#2f3640',
  textColor: '#f5f6fa',
  accentColor: '#44bd32'
}